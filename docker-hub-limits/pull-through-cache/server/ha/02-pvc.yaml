---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pull-through-pvc
spec:
  storageClassName: longhorn-nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
